FROM python:3.11-slim

RUN apt-get update && \
    apt-get install -y --no-install-recommends \
        build-essential \
        curl && \
    rm -rf /var/lib/apt/lists/*

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

RUN pip install https://github.com/explosion/spacy-models/releases/download/pl_core_news_lg-3.7.0/pl_core_news_lg-3.7.0-py3-none-any.whl

COPY . .

RUN chmod +x ./run.sh

HEALTHCHECK --interval=30s --timeout=10s --start-period=40s --retries=3 \
  CMD curl -f http://localhost:${PORT:-10000}/health || exit 1

EXPOSE 10000

CMD ["./run.sh"]
```

---

## **OPCJA 2: W requirements.txt (CZYSTSZA)**

### **requirements.txt:**
```
Flask==3.0.0
Flask-CORS==4.0.0
gunicorn==21.2.0
firebase-admin==6.3.0
spacy==3.7.2
https://github.com/explosion/spacy-models/releases/download/pl_core_news_lg-3.7.0/pl_core_news_lg-3.7.0-py3-none-any.whl
language-tool-python==2.8.1
textstat==0.7.3
pysbd==0.3.4
rapidfuzz==3.5.2
requests==2.31.0
google-generativeai==0.3.1
python-dotenv==1.0.0
pytest==7.4.3
black==23.12.1
